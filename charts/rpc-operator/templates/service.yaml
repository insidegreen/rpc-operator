apiVersion: v1
kind: Service
metadata:
  name: {{ include "rpc-operator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rpc-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: api
      port: {{ .Values.service.port }}
      targetPort: api
      protocol: TCP
  selector:
    {{- include "rpc-operator.selectorLabels" . | nindent 4 }}
