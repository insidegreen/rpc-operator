# Configure a Cluster

Configure the RPC Operator for your Kubernetes environment.

## Authentication

Enable token-based or OIDC authentication.

## Monitoring

Connect Prometheus for pipeline metrics.

## Namespace Allowlist

Restrict which namespaces the operator watches.
