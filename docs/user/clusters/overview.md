# PipelineCluster Overview

Understand PipelineCluster and stream-mode pipelines.

## What is a PipelineCluster?

A PipelineCluster is a group of pods running interconnected Redpanda Connect instances for distributed stream processing.

## Stream Pipelines

Pipelines deployed on a cluster run in stream mode.
